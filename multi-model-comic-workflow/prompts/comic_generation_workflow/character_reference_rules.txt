CHARACTER REFERENCE RULES:
- Treat every uploaded character image as a recurring identity reference.
- Match core facial traits as closely as the model allows: eyes, nose, mouth, hairline, face shape, skin tone, and other distinctive features.
- Keep each referenced character individually recognizable across all 5 panels.
- Apply the selected comic style to rendering, pose, action, and costume details without losing identity.
- Do not merge, swap, or drift the referenced characters' facial identities.

GENERAL NOTES:
- Reference images are higher priority than casual stylistic variation.
- If the provider supports reference images, pass them as model-side image references in addition to prompt text.
- If the provider does not support reference images, keep these rules in the prompt and store the images for future providers.
